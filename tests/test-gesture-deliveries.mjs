import assert from 'node:assert/strict';import {driveComparison} from '../dist/realism/GestureAcceptance.js';
const d=driveComparison(),names=['straight','offdrive','cover','square'];for(let i=0;i<4;i++){const a=d.attempts[i],p=a.frames[43].pose;assert.equal(p.intent.candidates[0].id,names[i]);if(i){assert.notEqual(p.kinematics.frontFoot.x,d.attempts[i-1].frames[43].pose.kinematics.frontFoot.x);assert.notEqual(p.humanTarget.top.x,d.attempts[i-1].frames[43].pose.humanTarget.top.x);}assert.ok(a.events.some(e=>e.type==='BallReleased'));}console.log('PASS four identical full deliveries produce different human feet and hand channels');
